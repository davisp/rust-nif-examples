{pre_hooks, [
    {compile, "cargo rustc --manifest-path=crates/basic/Cargo.toml --release -- --codegen link-args='-flat_namespace -undefined suppress'"},
    {compile, "mkdir -p priv"},
    {compile, "sh -c \"cp $(cat crates/basic/libpath) priv/libbasic.so\""}
]}.
